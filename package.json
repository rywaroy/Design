{
  "name": "design",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel --filter ./apps/** run dev",
    "dev:f": "pnpm --filter design-frontend dev",
    "dev:b": "pnpm --filter design-backend dev"
  }
}
